# Screenshots

Add screenshots of the application here:

- `board.png` - Main board view with multiple lists
- `create-task.png` - Task creation form
- `drag-drop.png` - Drag and drop in action
- `empty-state.png` - Empty state view

To capture screenshots:
1. Run the application (`npm run dev` in both server and web directories)
2. Open http://localhost:5173
3. Take screenshots of the key features
4. Save them in this directory with the names listed above
